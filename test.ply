package main

import "fmt"

func main() {
	m1 := map[int]int{1: 1}
	m2 := map[int]int{2: 2}
	fmt.Println(merge(m1, m2))

	xs := []int{1, 2, 3, 4, 5}
	xs = xs.filter(func(x int) bool { return x > 2 })
	fmt.Println(xs)
}